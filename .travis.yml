language: cpp
sudo: required
compiler:
  - clang
  - gcc
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libpng-dev
install:
  - mkdir build && cd build && cmake .. && make 
script:
  - cd test && testall
