language: cpp
sudo: required
compiler:
  - clang
  - gcc
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libm4ri-dev
install:
  - mkdir build && cd build && cmake .. && make 
script:
  - cd test && testall
